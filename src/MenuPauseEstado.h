#pragma once

#include "stdafx.h"
#include "MenuEstado.h"

class __attribute__((unused)) MenuPauseEstado : public MenuEstado {
public:
    MenuPauseEstado();

    __attribute__((unused)) MenuPauseEstado(const MenuPauseEstado &orig);

    virtual ~MenuPauseEstado();

private:

};

